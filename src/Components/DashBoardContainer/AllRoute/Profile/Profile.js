import React from 'react';
import UserInfo from '../../../Authentication/UserInfo';

const Profile = () => {
    return (
        <div>
            <UserInfo />
        </div>
    );
};

export default Profile;